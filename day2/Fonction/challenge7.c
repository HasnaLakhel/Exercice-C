#include <stdio.h>

int Inversion(char CH [20]){
    char CHI[20] ; 
    for ( i = 0; i < 20; i++)
    {
        
    }
    
    return CHI;
}
 
int main(){
    int CH[20];
    printf("entre une chaîne de caractères");
    scanf("%s",&CH);
    printf(" la chaîne inversée %s ",Inversion(CH));
   return 0;
}