#include <stdio.h>

int main() {
    int lo , la , surface;
    printf("entre la longueur\n");
    scanf("%d",&lo);
    printf("entre la largeur \n ");
    scanf("%d",&la);
    surface=lo*la;
    printf("la surface :%d",surface);

    return 0;
}